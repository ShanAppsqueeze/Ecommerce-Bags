/** @type {import('next').NextConfig} */
const nextConfig = {

      images: {
    domains: [
      'images.unsplash.com',
      'i.ibb.co',
      'randomuser.me'
    ],
  },
};

export default nextConfig;
